cargo-features = ["trim-paths"]

[package]
name = "fseedemo"
edition = "2021"
authors = ["XtrLumen"]

[dependencies]
libloading = "0.9.0"

[profile.release]
lto = true
strip = true
debug = false
panic = "abort"
opt-level = "z"
trim-paths = "all"
incremental = false
codegen-units = 1